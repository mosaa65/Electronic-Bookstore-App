# 📋 خطة تنفيذ التحسينات الشاملة

## نظرة عامة

تحسينات شاملة لتطبيق متجر الكتب الإلكتروني تشمل:
1. **دعم ثنائي اللغة** (عربي/إنجليزي)
2. **نظام خطوط مركزي** (تغيير واحد يؤثر على كل التطبيق)
3. **إعادة تصميم واجهات المدير** (لوحة تحكم احترافية)
4. **نظام تسجيل المدراء الآمن** (أول مدير يدوي + المدراء عبر لوحة التحكم)

---

## المرحلة 1: نظام ثنائي اللغة 🌐

### الملفات الجديدة

#### [NEW] `lib/core/localization/app_localizations.dart`
ملف الترجمات الرئيسي مع دعم اللغتين:
```dart
class AppLocalizations {
  static Map<String, Map<String, String>> _localizedValues = {
    'ar': { /* النصوص العربية */ },
    'en': { /* النصوص الإنجليزية */ },
  };
  
  String get(String key) => _localizedValues[locale][key];
}
```

#### [NEW] `lib/core/localization/locale_provider.dart`
Provider لإدارة اللغة:
```dart
class LocaleProvider extends ChangeNotifier {
  Locale _locale = Locale('ar'); // العربية افتراضياً
  
  void setLocale(Locale locale) {
    _locale = locale;
    notifyListeners();
  }
}
```

#### [MODIFY] [lib/core/constants/app_strings.dart](file:///c:/Users/mousa/Desktop/rakan/ebook_store/lib/core/constants/app_strings.dart)
تحويله لدعم اللغتين:
- إضافة قاموس عربي وإنجليزي
- دالة [tr(key)](file:///c:/Users/mousa/Desktop/rakan/ebook_store/lib/core/constants/app_strings.dart#1-100) للوصول للنص حسب اللغة الحالية

#### [MODIFY] [lib/main.dart](file:///c:/Users/mousa/Desktop/rakan/ebook_store/lib/main.dart)
- إضافة `LocaleProvider` للـ MultiProvider
- إضافة `locale` و `supportedLocales` لـ MaterialApp
- إضافة `LocalizationDelegate`

---

## المرحلة 2: نظام الخطوط المركزي ✍️

### [NEW] `lib/core/theme/app_typography.dart`
```dart
class AppTypography {
  // الخط الأساسي - تغييره هنا يغير كل التطبيق
  static String get primaryFontFamily => 'Cairo';
  static String get englishFontFamily => 'Roboto';
  
  // أحجام الخطوط
  static const double displayLarge = 32;
  static const double displayMedium = 24;
  static const double bodyLarge = 16;
  static const double bodyMedium = 14;
  
  // دوال جاهزة للاستخدام
  static TextStyle headline({bool isArabic = true}) => ...
  static TextStyle body({bool isArabic = true}) => ...
}
```

### [NEW] `lib/core/theme/app_theme.dart`
ملف موحد للثيم:
```dart
class AppTheme {
  static ThemeData lightTheme(Locale locale) => ThemeData(
    textTheme: AppTypography.getTextTheme(locale),
    // باقي إعدادات الثيم
  );
  
  static ThemeData darkTheme(Locale locale) => ...
}
```

### [MODIFY] [lib/main.dart](file:///c:/Users/mousa/Desktop/rakan/ebook_store/lib/main.dart)
- استبدال تعريف الثيم المباشر بـ `AppTheme.lightTheme(locale)`

---

## المرحلة 3: واجهات المدير الاحترافية 👨‍💼

### [MODIFY] [lib/presentation/screens/admin/admin_dashboard.dart](file:///c:/Users/mousa/Desktop/rakan/ebook_store/lib/presentation/screens/admin/admin_dashboard.dart)
إعادة تصميم كاملة مع:

**الإحصائيات المخطط لها:**
- 📚 إجمالي الكتب
- 👥 عدد المستخدمين
- 💰 إجمالي المبيعات
- ⭐ أعلى تقييم
- 📈 رسم بياني للمبيعات

**الأقسام الجديدة:**
- إدارة الكتب (موجود - تحسين)
- إدارة المستخدمين (جديد)
- إدارة المدراء (جديد)
- الإحصائيات والتقارير

**التنقل:**
- شريط جانبي (Drawer) أو TabBar للأقسام
- تصميم عصري مع البطاقات والرسوميات

### [NEW] `lib/presentation/screens/admin/manage_admins_screen.dart`
شاشة إدارة المدراء:
- قائمة المدراء الحاليين
- زر إضافة مدير جديد
- تعديل/حذف صلاحيات المدير

### [NEW] `lib/presentation/screens/admin/admin_statistics_screen.dart`
شاشة الإحصائيات:
- رسوم بيانية للمبيعات
- أكثر الكتب مبيعاً
- إحصائيات المستخدمين

---

## المرحلة 4: نظام تسجيل المدراء الآمن 🔐

### المنطق الجديد

> [!IMPORTANT]
> **نظام المدراء الآمن:**
> - **أول مدير**: تُنشئه أنت يدوياً في Firebase Console أو بكود التحقق
> - **بقية المدراء**: يُنشئهم المدير من لوحة التحكم فقط

### [MODIFY] [lib/presentation/screens/auth/register_screen.dart](file:///c:/Users/mousa/Desktop/rakan/ebook_store/lib/presentation/screens/auth/register_screen.dart)
- **إزالة** خيار التسجيل كمدير من شاشة التسجيل العامة
- التسجيل العام للمستخدمين العاديين فقط

### [NEW] `lib/presentation/screens/admin/create_admin_screen.dart`
شاشة إنشاء مدير جديد (للمدير فقط):
```dart
class CreateAdminScreen extends StatefulWidget {
  // يمكن للمدير الحالي إنشاء مدير جديد
  // الحقول: الاسم، البريد، كلمة المرور
}
```

### [MODIFY] [lib/data/repositories/auth_repository.dart](file:///c:/Users/mousa/Desktop/rakan/ebook_store/lib/data/repositories/auth_repository.dart)
إضافة دالة `createAdminByAdmin()`:
```dart
Future<UserModel?> createAdminByAdmin({
  required String email,
  required String password,
  required String displayName,
  required String createdBy, // UID المدير الذي أنشأ هذا الحساب
}) async {
  // إنشاء الحساب مع role = 'admin'
}
```

### [MODIFY] [lib/presentation/providers/auth_provider.dart](file:///c:/Users/mousa/Desktop/rakan/ebook_store/lib/presentation/providers/auth_provider.dart)
إضافة دالة لإنشاء مدير من قبل مدير آخر

---

## هيكل الملفات النهائي

```
lib/
├── core/
│   ├── constants/
│   │   ├── app_colors.dart        ✅ موجود
│   │   ├── app_strings.dart       📝 تعديل
│   │   └── app_routes.dart        ✅ موجود
│   ├── localization/              🆕 جديد
│   │   ├── app_localizations.dart
│   │   └── locale_provider.dart
│   ├── theme/                     🆕 جديد
│   │   ├── app_typography.dart
│   │   └── app_theme.dart
│   └── widgets/                   ✅ موجود
│
├── presentation/
│   ├── screens/
│   │   ├── admin/
│   │   │   ├── admin_dashboard.dart      📝 تعديل كبير
│   │   │   ├── add_edit_book_screen.dart ✅ موجود
│   │   │   ├── manage_admins_screen.dart 🆕 جديد
│   │   │   ├── create_admin_screen.dart  🆕 جديد
│   │   │   └── admin_statistics_screen.dart 🆕 جديد
│   │   └── auth/
│   │       └── register_screen.dart      📝 تعديل
│   └── providers/
│       ├── locale_provider.dart          🆕 جديد
│       └── auth_provider.dart            📝 تعديل
│
└── main.dart                             📝 تعديل
```

---

## خطة التحقق (Verification Plan)

### 1. اختبار اللغة
**اختبار يدوي:**
1. شغّل التطبيق: `flutter run`
2. اذهب للإعدادات (الملف الشخصي)
3. غيّر اللغة من عربي لإنجليزي
4. تحقق أن جميع النصوص تغيرت
5. أغلق التطبيق وافتحه مرة أخرى - يجب أن تبقى اللغة محفوظة

### 2. اختبار الخطوط
**اختبار يدوي:**
1. غيّر اسم الخط في `app_typography.dart` من `Cairo` لـ `Tajawal`
2. شغّل التطبيق
3. تحقق أن الخط تغير في جميع الشاشات

### 3. اختبار لوحة المدير
**اختبار يدوي:**
1. سجّل دخول كمدير
2. تحقق من ظهور الإحصائيات (عدد الكتب، المستخدمين، المبيعات)
3. تحقق من وجود قسم "إدارة المدراء"
4. جرب إنشاء مدير جديد

### 4. اختبار إنشاء المدراء
**اختبار يدوي:**
1. سجّل دخول كمدير
2. اذهب لـ "إدارة المدراء"
3. أنشئ حساب مدير جديد
4. سجّل خروج وسجّل دخول بالحساب الجديد
5. تحقق أن الحساب الجديد يدخل للوحة التحكم

### 5. اختبار أمان التسجيل
**اختبار يدوي:**
1. سجّل خروج
2. اذهب لشاشة التسجيل
3. تحقق أنه **لا يوجد** خيار "التسجيل كمدير"
4. سجّل حساب جديد - يجب أن يكون مستخدم عادي فقط

---

## الأولوية والترتيب

| المرحلة | الأولوية | الوقت المتوقع |
|---------|----------|---------------|
| نظام الخطوط | 🔴 عالية | 1 ساعة |
| نظام اللغة | 🟡 متوسطة | 2-3 ساعات |
| نظام المدراء | 🔴 عالية | 2 ساعات |
| لوحة المدير | 🟡 متوسطة | 3-4 ساعات |

---

## ملاحظات مهمة

> [!CAUTION]
> **بخصوص أول مدير:**
> - يمكنك إنشاء أول مدير بتعديل بيانات المستخدم في Firebase Console
> - أو تسجيل حساب عادي ثم تغيير `role` من `user` لـ `admin` في Firestore
> - بعد ذلك كل المدراء الجدد يُنشؤون من لوحة التحكم

> [!TIP]
> **بخصوص تبديل اللغة:**
> - سنحفظ اختيار اللغة في SharedPreferences
> - عند فتح التطبيق يقرأ اللغة المحفوظة

---

## هل توافق على هذه الخطة؟

أخبرني إذا تريد:
1. ✅ الموافقة والبدء بالتنفيذ
2. ✏️ تعديل أي جزء
3. ➕ إضافة ميزات أخرى
4. ❓ توضيح أي نقطة
